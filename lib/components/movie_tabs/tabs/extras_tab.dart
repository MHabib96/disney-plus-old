import 'package:flutter/material.dart';

class ExtrasTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Extras Tab', style: TextStyle(color: Colors.white)));
  }
}
